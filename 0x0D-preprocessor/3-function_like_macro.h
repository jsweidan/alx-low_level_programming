#ifndef MACROLIKE_H
#define MACROLIKE_H

/**
 *File - 3-function_like_macro
 *
 *@x: input value
 *
 *Description: macroi that computes the absolute value of a number
 */

#define ABS(x)  ((x) < 0 ? -(1) * (x) : (x))

#endif
